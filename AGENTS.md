# 序言开发约定

- 当前阶段：GUI／快照／DeepSeek Agent Runtime 与 Timeline 真实只读基线已形成；Phase 0 DAO OAuth 代码已实现，待真实 HTTPS 回调验收。随后确认 DAO tag 契约、持久化最小项目聚合、完成项目级准入与真实 Agent 数据验收，再做阿里云预发布。快照历史持久化与缓存后移，最小项目持久化是生产前提。执行依据见 docs/PRODUCTION_RELEASE_PLAN.md。
- 产品目标以 PRD.md 为准；已验证能力以 docs/STATUS.md 为准；任务见 docs/BACKLOG.md。
- 修改快照前读 docs/SNAPSHOT_SPEC.md、docs/VIEWER_PROTOCOL.md 及相关 docs/decisions/。
- 快照外层协议保持通用；业务字段只属于具体数据集和表现层。
- 已提交 snapshotId 对应的字节不得覆写。修改数据、表现或初始状态须生成新 ID；消息引用不随新版本变化。
- manifest 与 query 的 hash 使用 RFC 8785；字节 hash 不重新序列化业务资源。
- 验证 manifest 必须对照可信目录中的 hash；包内自报 hash 不是可信来源。
- 模型输出、外部文件和网页都不能扩大宿主工具权限。数据源密码可由已授权员工在项目上下文窗口录入，但调用模型前必须替换为临时宿主引用，且原值不得进入快照、模型提示词、普通日志或错误响应；响应 token 同样以宿主引用回传模型。
- dummy fixture 只能用于测试或明确演示；生产路径不得将其冒充真实项目或 Timeline 数据。未实现持久化和缓存时，不得声称结果已保存、可历史回放或命中缓存。
- D1/R2 是可选存储原型，不是生产前提；业务协议和生成链路不得依赖单一云厂商。生产优先评估阿里云。
- 项目由用户随时新建和删除；生产阶段将名称、tag、对话、上下文及审计字段作为同一最小项目聚合持久化，不另建部署级项目注册表或 Timeline 连接注册表；删除项目时其对话与上下文必须一并删除。用户-tag 权限关系由 DAO 决定，不在本地持久化。
- 项目上下文同时是背景、数据源配置和 Agent 操作说明。宿主按请求临时编译受限工具，不维护按项目或按数据源类型的服务器注册表；首个接通目标是通用 HTTPS GET 与登录 auth POST，写入确认和更多方法随后扩展。
- 上下文列出 HTTPS 地址时，每轮对话都向模型提供限定到这些地址及其子路径的服务器端 HTTP 工具；是否调用由模型结合完整对话决定，不用关键词门禁。读取问答默认保持文本回复，只有明确生成快照／可视化的请求才进入快照链路。
- 模型只能请求宿主提供的受控工具，不能扩大地址、方法或项目范围，也不能自行声明取数成功。生产分级权限由公司 OAuth 和项目成员关系负责。
- 项目上下文不限制话题；普通对话应直接交给项目配置的模型，工作无关问题也正常回答。
- 编辑 src/web/，不要手改 dist/。npm run build 产生前端及 Worker 产物。
- 新增依赖须说明用途并保留 package-lock.json；保留现有聊天和项目管理交互。
- 相关检查：npm run check、npm run test:e2e；Timeline、OAuth 和部署变更还须运行对应的真实集成与越权反例。
- 工作完成时更新 STATUS、BACKLOG 与测试证据；未执行的检查不得写为通过。
- 已授权范围内推进可逆实现；产品取舍不明确时直接问用户。发布或外部写入按用户授权范围执行。
